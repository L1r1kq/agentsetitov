# C4 — Agentse

## Level 1. System Context

```mermaid
C4Context
title Agentse — System Context
Person(user, "Оператор", "Студент / инженер")
System(agentse, "Agentse", "Мультиагентная оркестрация, память, evals")
System_Ext(ollama, "Ollama", "Локальный инференс OSS LLM")
System_Ext(sandbox, "Sandbox runtime", "Изолированный Python")
System_Ext(obs, "Observability stack", "Prom/Grafana/Alertmanager/Tempo/Langfuse")
Rel(user, agentse, "задачи, evals, просмотр памяти")
Rel(agentse, ollama, "chat completions")
Rel(agentse, sandbox, "exec code")
Rel(agentse, obs, "метрики, логи, трейсы, scores")
```

## Level 2. Containers

```mermaid
C4Container
title Agentse — Containers
Person(user, "Оператор")
Container(web, "Web/CLI", "FastAPI + Typer", "Вход в систему")
Container(graph, "Agent graph", "LangGraph", "Supervisor и специалисты")
Container(mem, "Memory store", "SQLite + Markdown", "HCM: слои памяти")
Container(tools, "Tool runtime", "Python", "skills, workspace, calc")
ContainerDb(files, "Workspace", "Markdown", "SOUL, MEMORY, daily")
System_Ext(ollama, "Ollama")
System_Ext(sandbox, "Docker sandbox")
System_Ext(prom, "Prometheus")
System_Ext(tempo, "Tempo / file traces")
Rel(user, web, "HTTP / CLI")
Rel(web, graph, "invoke(task)")
Rel(graph, ollama, "промпты ролей")
Rel(graph, tools, "tool calls")
Rel(tools, mem, "search/write")
Rel(tools, files, "read identity / skills")
Rel(tools, sandbox, "restricted exec")
Rel(graph, prom, "/metrics")
Rel(graph, tempo, "spans")
```

## Level 3. Components (граф)

```mermaid
C4Component
title Agent graph — Components
Container_Boundary(g, "LangGraph StateGraph") {
  Component(sup, "Supervisor", "hybrid router")
  Component(pl, "Planner", "декомпозиция")
  Component(rs, "Researcher", "память и пробелы")
  Component(bd, "Builder", "артефакт / sandbox")
  Component(cr, "Critic", "вердикт")
  Component(cu, "Curator", "консолидация")
}
Component(pol, "Policy", "детерминированный якорь")
Component(llm, "OllamaClient", "httpx")
Rel(sup, pol, "merge_route")
Rel(sup, llm, "JSON next")
Rel(sup, pl, "handoff")
Rel(sup, rs, "handoff")
Rel(sup, bd, "handoff")
Rel(sup, cr, "handoff")
Rel(sup, cu, "handoff")
Rel(pl, llm, "plan JSON")
Rel(rs, llm, "findings JSON")
Rel(bd, llm, "artifact JSON")
Rel(cr, llm, "verdict JSON")
Rel(cu, llm, "facts JSON")
```

## Level 4. Code (намеренно тонкий)

На L4 достаточно указателей в репозитории: `src/agentse/graph.py` (узлы), `agents/policy.py` (якорь), `memory/store.py` (HCM), `tools/sandbox.py` (изоляция). C4 не заменяет чтение этих файлов на защите.
